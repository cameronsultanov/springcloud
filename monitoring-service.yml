eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    instance-id: monitoring-service

security:
  user:
    name: kamron
    password: admin

server:
  port: 9899
